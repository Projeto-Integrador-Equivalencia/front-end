services:
  app:
    build: .
    ports:
      - "4000:3000"
    volumes:
      - .:/app
    environment:
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
      - FAST_REFRESH=true
    command: npm run dev -- --hostname 0.0.0.0